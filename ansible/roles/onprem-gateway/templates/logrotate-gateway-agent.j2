/var/log/naisdevice/*.log /var/log/naisdevice/*.json {
        daily
        missingok
        rotate 7
        compress
        notifempty
        create 640 root google-sudoers

        postrotate
                systemctl start gateway-agent
        endscript

        prerotate
                systemctl stop gateway-agent
        endscript
}
